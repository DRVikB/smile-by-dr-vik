import type { SmileSettings } from "../types";
export function buildSmileInstruction(s: SmileSettings): string {
  return `Create a photorealistic cosmetic dentistry communication preview from the provided frontal smiling photograph. Modify only ${s.teeth} upper anterior teeth, symmetrically around the midline (FDI tooth numbers: ${s.selectedTeeth.join(", ")}). Treatment material: ${s.treatment}. Dentist-selected current shade: ${s.currentShade}; requested target shade: ${s.targetShade}. This is a supplied shade reference, not a diagnosis from the photograph. Tooth morphology: ${s.shape}. Transformation intensity: ${s.intensity}/100, where 0 is subtle and natural, and 100 is more enhanced while remaining anatomically believable. Preserve facial identity, facial proportions, lips, skin, gingiva, background, pose, camera framing, lighting, and all untreated teeth exactly. Do not edit the gingiva; if the requested result would require gingival editing, reduce the tooth changes instead. Preserve realistic enamel translucency, texture, surface reflections, interdental contacts and individual character. Avoid CGI, flat opaque white teeth, excessive uniformity and unnatural proportions. Return only the edited photograph at the same aspect ratio and composition.`;
}
